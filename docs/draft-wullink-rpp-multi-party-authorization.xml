<?xml version="1.0" encoding="utf-8"?>
<!-- name="GENERATOR" content="github.com/mmarkdown/mmark Mmark Markdown Processor - mmark.miek.nl" -->
<rfc version="3" ipr="trust200902" docName="draft-wullink-rpp-multi-party-authorization-00" submissionType="IETF" category="std" xml:lang="en" xmlns:xi="http://www.w3.org/2001/XInclude" indexInclude="true" tocDepth="4">

<front>
<title abbrev="Multi-Party Authorization for RPP">Multi-Party Authorization for RESTful Provisioning Protocol (RPP)</title><seriesInfo value="draft-wullink-rpp-multi-party-authorization-00" stream="IETF" status="standard" name="Internet-Draft"></seriesInfo>
<author initials="M." surname="Wullink" fullname="Maarten Wullink"><organization>SIDN Labs</organization><address><postal><street></street>
</postal><email>maarten.wullink@sidn.nl</email>
<uri>https://sidn.nl/</uri>
</address></author><author initials="P." surname="Kowalik" fullname="Pawel Kowalik"><organization>DENIC</organization><address><postal><street></street>
</postal><email>pawel.kowalik@denic.de</email>
<uri>https://denic.de/</uri>
</address></author><date year="2026" month="November" day="14"></date>
<area>Internet</area>
<workgroup>Network Working Group</workgroup>

<abstract>
<t>The traditional Registry, Registrar and Registrant (RRR) model for domain name management is evolving to include third-party service providers, such as DNS hosting providers. This document describes a generic multi-party authorization flow that allows registries to securely delegate domain management functions to accredited third-party service providers, while ensuring that the registrant's explicit consent is obtained before any RPP operations, described in <xref target="I-D.ietf-rpp-core"></xref>, are executed.</t>
<t><strong>TODO</strong></t>
</abstract>

</front>

<middle>

<section anchor="introduction"><name>Introduction</name>
<t>The generic multi-party authorization flow described in this document allows registries to securely delegate RPP object operations to accredited third-party service providers, while ensuring that the registrar and registrant's explicit consent is obtained before any RPP operations, described in <xref target="I-D.ietf-rpp-core"></xref>, are executed. The registry and the 3rd party MUST have a pre-established trust relationship, how this trust is established is out of scope for this document. The 3rd party does not have a direct trust relationship with the registrar, but the registrar trusts the registry to only accept requests from accredited 3rd parties. How to secure the HTTP endpoints used in the multi-party authorization flow is out of scope for this document, but it is RECOMMENDED that OAuth 2.0 for RPP, as described in <xref target="I-D.wullink-rpp-oauth2"></xref>, be used.</t>
<t><strong>TODO</strong> what about registries that allow direct registr access, without a registrar?</t>
</section>

<section anchor="terminology"><name>Terminology</name>
<t>In this document the following terminology is used.</t>
<t>URL - A Uniform Resource Locator as defined in <xref target="RFC3986"></xref>.</t>
<t>Resource - An object having a type, data, and possible relationship to other resources, identified by a URL.</t>
<t>RPP client - An HTTP user agent performing an RPP request</t>
<t>RPP server - An HTTP server responsible for processing requests and returning results in any supported media type.</t>
<t>Registry - The authoritative source of truth for registration data, responsible for maintaining the database of shared objects and providing access to it through the RPP server.</t>
<t>Registrar - The entity responsible for managing the registration of objects, such as a domain name, on behalf of registrants, typically interacting with both the registrant and the registry.</t>
<t>Registrant - The individual or organization that has registered an object in the registry database.</t>
<t>3rd party - A service provider that is accredited by the registry to perform domain management operations on behalf of the registrant.</t>
</section>

<section anchor="conventions-used-in-this-document"><name>Conventions Used in This Document</name>
<t>The key words &quot;MUST&quot;, &quot;MUST NOT&quot;, &quot;REQUIRED&quot;, &quot;SHALL&quot;, &quot;SHALL NOT&quot;,&quot;SHOULD&quot;, &quot;SHOULD NOT&quot;, &quot;RECOMMENDED&quot;, &quot;MAY&quot;, and &quot;OPTIONAL&quot; in this document are to be interpreted as described in <xref target="RFC2119"></xref>.</t>
<t>In examples, indentation and white space are provided only to illustrate element relationships and are not REQUIRED features of the protocol.</t>
</section>

<section anchor="architectural-overview"><name>Architectural Overview</name>
<t>RPP enables registries to securely delegate domain management functions to accredited third-party service providers, such as DNS hosting providers.</t>

<section anchor="authorization-request"><name>Authorization Request</name>
<t>The authorization of such a delegated operation requires the explicit participation of four parties: the registrant, the third party performing the operation, the registry, and the registrar managing the object resource. The registry and the registrar each apply their own digital signature to the authorization request, the operation MUST only be executed once both signatures have been verified.</t>
<t>The diagram in <xref target="fig-mpa-flow"></xref> illustrates the multi-party authorization flow:</t>
<figure anchor="fig-mpa-flow"><name>multi-party Authorization Flow </name>
<sourcecode type="ascii"><![CDATA[   Registrant    Third Party     Registry        Registrar
     |               |               |               |
     | 1. Initiate   |               |               |
     |  operation    |               |               |
     +-------------->|               |               |
     |               |               |               |
     |               | 2. Create     |               |
     |               |    request    |               |
     |               |               |               |
     |               | 3. Send       |               |
     |               |  request      |               |
     |               +-------------->|               |
     |               |               |               |
     |               | 4.            |               |
     |               |  signed req. +|               |
     |               |  registrar    |               |
     |               |  redirect URL |               |
     |               |<--------------|               |
     |               |               |               |
     | 5. Redirect   |               |               |
     |  to registrar |               |               |
     |  URL          |               |               |
     |               |               |               |
     |<--------------|               |               |
     |               |               |               |
     | 6. Follow     |               |               |
     |  redirect     |               |               |
     +---------------------------------------------->|
     |               |               |               |
     | 7. Validate & |               |               |
     |    Approve    |               |               |
     | 8. Redirect   |               |               |
     |<----------------------------------------------|
     |               |               |               |
     | 9. Follow     |               |               |
     |  redirect     |               |               |
     +-------------->|               |               |
     |               |               |               |
     |               | 10. Submit    |               |
     |               |  request      |               |
     |               +-------------->|               |
     |               |               |               |
     |               |               | 11. Verify all|
     |               |               |  signatures & |
     |               |               |  create       |
     |               |               |  authorisation|
     |               |               |               |
     | 12. Auth.     |               |               |
     |  granted      |               |               |
     |<--------------|               |               |
     |               |               |               |
]]>
</sourcecode>
</figure>
<t>The individual steps in the diagram <xref target="fig-mpa-flow"></xref> performed by each party are described below:</t>

<ol spacing="compact">
<li>The client connects to the third party to initiate a domain management operation that requires authorization from the registry and the registrar.</li>
<li>The third party's backend constructs a request describing the requested operation, expressing the third party's intent to perform the operation on behalf of the registrant.</li>
<li>The third party sends the request to the RPP authorization endpoint.</li>
<li>The registry checks if the third party is accredited to act on the object and validates the request, then it signs the request. The registry returns the request, together with the URL of the registrar's web-based consent screen to which the third party must redirect the client's browser for transaction approval.</li>
<li>The third party redirects the client's browser to the registrar's consent screen at the URL provided by the registry, conveying the signed request as a parameter of the redirect.</li>
<li>The client's browser follows the redirect to the registrar.</li>
<li>The registrar validates the signature of the registry, and checks if the request is valid and allowed per the registrar's policies. The registrar then presents the requested operation to the registrant for approval. If the registrant approves, the registrar signs the response with its own signature.</li>
<li>The registrar redirects the client's browser back to the third party.</li>
<li>The client's browser follows the redirect, delivering the request signed by the registry and the registrar to the third party.</li>
<li>The third party submits the authorization request to the registry, including the signatures of both the registry and the registrar.</li>
<li>The registry verifies the signatures of the registry itself and the registrar. The registry creates an Authorisation Data Object, which is stored in the registry's database and used to authorize future operations.</li>
<li>The third party informs the client that the requested authorisation has been granted.</li>
</ol>
</section>

<section anchor="authorization-usage"><name>Authorization Usage</name>
<t>After the multi-party authorization flow is completed, the registry MUST use the Authorisation Data Object to authorize an operation request by a 3rd party. If the object in the request of a 3rd party is linked to a valid Authorisation Data Object, the registry MUST execute the requested operation and return the result to the 3rd party. If there is no Authorisation Data Object or it is invalid, the registry MUST reject the request and return an appropriate error response to the 3rd party.</t>
<t><strong>TODO</strong> add diagragm showing how the Authorisation Data Object is used to authorize an operation request by a 3rd party.</t>
</section>

<section anchor="authorization-management"><name>Authorization Management</name>
<t>An authorization granted by the registry MAY be terminated by the 3rd party, the registrar or the registry at any time, before the expiration of the Authorisation Data Object.</t>
<t><strong>TODO</strong> add diagragm showing how the Authorisation Data Object is revoked by the registrar or registry or rescinded by the 3rd party.</t>
</section>
</section>

<section anchor="data-objects"><name>Data Objects</name>
<t><strong>TODO</strong> here or in data objects doc? (now in data objects doc)</t>

<section anchor="component-data-objects"><name>Component Data Objects</name>

<section anchor="public-key-data-object"><name>Public Key Data Object</name>
<t><strong>TODO</strong></t>
</section>

<section anchor="signature-data-object"><name>Signature Data Object</name>
<t><strong>TODO</strong></t>
</section>

<section anchor="approval-data-object"><name>Approval Data Object</name>
<t><strong>TODO</strong></t>
</section>
</section>

<section anchor="authorisation-data-object"><name>Authorisation Data Object</name>

<section anchor="authorisation-elements"><name>Elements</name>
<t>This section describes the data elements of the Authorisation Data Object, as defined in <xref target="I-D.ietf-rpp-data-objects"></xref>.</t>

<ul spacing="compact">
<li><tt>transactionType</tt>: The type of transaction for the authorisation request. MUST be one of the values registered in the &quot;RPP Multi-Party Transaction Types&quot; registry <xref target="tbl-rpp-transaction-types"></xref>. Every transaction type is associated with a specific RPP operation, and the <tt>data</tt> property of the authorisation request MUST contain the RPP request body for that operation.</li>
<li><tt>id</tt>: The identifier for the authorisation request.</li>
<li><tt>timestamp</tt>: The time the authorisation request was created.</li>
<li><tt>expiration</tt>: The time the authorisation request expires. MUST be later than <tt>timestamp</tt>.</li>
<li><tt>objectId</tt>: The identifier of the object the authorisation request pertains to.</li>
<li><tt>requestorId</tt>: The unique organisation identifier of the requestor.</li>
<li><tt>requestorName</tt>: The name of the requestor.</li>
<li><tt>approvalUrl</tt>: The URI to which the client should be redirected for multi-party approval. MUST only be used when the referenced organisation is a registrar or reseller supporting multi-party approval.</li>
<li><tt>returnUrl</tt>: The URI to which the client should be redirected after multi-party approval at the registrar. MUST only be used when the referenced organisation is a 3rd party supporting multi-party approval.</li>
<li><tt>usage</tt>: The usage type for the authorisation request. MUST be one of <tt>&quot;single-use&quot;</tt> or <tt>&quot;multi-use&quot;</tt>, the default being <tt>&quot;single-use&quot;</tt>.</li>
<li><tt>data</tt>: The data associated with the authorisation request, containing the RPP request body for the operation identified by <tt>transactionType</tt>; MUST be a valid RPP Data Object, see <xref target="transaction-types"></xref>.</li>
<li><tt>signatures</tt>: The digital signatures applied to the authorisation request, used to verify its authenticity and integrity, see <xref target="request-signing-and-verification"></xref>.</li>
<li><tt>approval</tt>: The approval information for the authorisation request.</li>
</ul>
</section>

<section anchor="authorisation-party-usage"><name>Usage of elements by parties</name>
<t>The Authorisation Data Object elements are set by different parties involved in the multi-party authorization flow, and read by others as needed to process or verify the transaction.</t>
<table anchor="tbl-authorisation-party-usage"><name>Party responsible for setting and reading each data element</name>
<thead>
<tr>
<th>Identifier</th>
<th>Set by</th>
<th>Read by</th>
</tr>
</thead>

<tbody>
<tr>
<td><tt>transactionType</tt></td>
<td>3rd party</td>
<td>Registry, Registrar</td>
</tr>

<tr>
<td><tt>id</tt></td>
<td>Registry</td>
<td>3rd party, Registrar</td>
</tr>

<tr>
<td><tt>timestamp</tt></td>
<td>Registry</td>
<td>3rd party, Registrar</td>
</tr>

<tr>
<td><tt>expiration</tt></td>
<td>Registry</td>
<td>3rd party, Registrar</td>
</tr>

<tr>
<td><tt>objectId</tt></td>
<td>3rd party</td>
<td>Registry, Registrar</td>
</tr>

<tr>
<td><tt>requestorId</tt></td>
<td>Registry</td>
<td>Registrar</td>
</tr>

<tr>
<td><tt>requestorName</tt></td>
<td>Registry</td>
<td>Registrar</td>
</tr>

<tr>
<td><tt>approvalUrl</tt></td>
<td>Registry</td>
<td>3rd party</td>
</tr>

<tr>
<td><tt>returnUrl</tt></td>
<td>Registry</td>
<td>Registrar</td>
</tr>

<tr>
<td><tt>usage</tt></td>
<td>3rd party</td>
<td>Registry, Registrar</td>
</tr>

<tr>
<td><tt>data</tt></td>
<td>3rd party</td>
<td>Registry, Registrar</td>
</tr>

<tr>
<td><tt>signatures</tt></td>
<td>Registry, Registrar</td>
<td>Registry, Registrar</td>
</tr>

<tr>
<td><tt>approval</tt></td>
<td>Registrar</td>
<td>Registry, 3rd party</td>
</tr>
</tbody>
</table></section>

<section anchor="json-schema"><name>JSON Schema</name>
<t>This section provides normative JSON Schema definitions for the transaction types defined in this document. All schemas use JSON Schema draft 2020-12 <xref target="JSON-SCHEMA"></xref>. The schema below represents the Authorisation Data Object defined in <xref target="I-D.ietf-rpp-data-objects"></xref> and described in <xref target="authorisation-elements"></xref>.</t>

<sourcecode type="json"><![CDATA[{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$ref": "#/$defs/authorisation.create",
  "$defs": {
    "authorisation.create": {
      "type": "object",
      "properties": {
        "@type": { "type": "string", "const": "authorisation" },
        "transactionType": { "type": "string", "enum": ["rpp:mpa-type:domain:ns-update", "rpp:mpa-type:domain:dnssec-update", "rpp:mpa-type:domain:transfer"] },
        "id": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" },
        "expiration": { "type": "string", "format": "date-time" },
        "objectId": { "type": "string" },
        "requestorId": { "type": "string" },
        "requestorName": { "type": "string" },
        "approvalUrl": { "type": "string", "format": "uri" },
        "returnUrl": { "type": "string", "format": "uri" },
        "usage": { "type": "string", "enum": ["single-use", "multi-use"] },
        "data": {
          "description": "The RPP JSON request body for the operation identified by transactionType"
        },
        "signatures": {
          "type": "array",
          "description": "An ordered list of signatures",
          "items": {
            "type": "object",
            "properties": {
              "keyId": { "type": "string" },
              "signedAt": {
                "type": "string",
                "format": "date-time",
                "description": "The date and time at which this signature was applied."
              },
              "signature": { "type": "string" }
            },
            "required": ["keyId", "signedAt", "signature"],
            "additionalProperties": false
          },
          "minItems": 0
        },
        "approval": {
          "type": "object",
          "properties": {
            "approved": { "type": "boolean" },
            "approvedBy": { "type": "string" },
            "reason": { "type": "string" },
            "timestamp": { "type": "string", "format": "date-time" }
          },
          "required": ["approved", "approvedBy", "timestamp"],
          "additionalProperties": false
        }
      },
      "required": ["@type", "transactionType", "id", "timestamp", "expiration", "objectId", "requestorId"]
    }
  }
}
]]>
</sourcecode>
<t><strong>TODO</strong> currently using single schema where depending on client , registry or registrar some fields are required and some are optional. Could also define separate schemas for each party.</t>
</section>
</section>

<section anchor="organization-data-object"><name>Organization Data Object</name>
<t>This specification extends the Organization type specified in <xref target="I-D.ietf-rpp-data-objects"></xref> to include two additional elements: <tt>approvalUrl</tt>, which is a URL that points to the registrar's web-based consent screen, and <tt>returnUrl</tt>, which is a URL to which the client's browser is redirected after the registrant has approved or denied the requested operation. The <tt>approvalUrl</tt> and <tt>returnUrl</tt> MUST be valid URLs, and they MUST use the HTTPS scheme.</t>
<t>The registry and the registrar MUST redirect the client's browser using a <tt>303 See Other</tt> response, with the <tt>Location</tt> header set to the URL of the registrar's consent screen, including base64 encoded representation of the Authorisation Data Object in the <tt>approval-state</tt> query parameter.</t>
<t>This specification does not define a specific format for the redirection URL, but it is RECOMMENDED that the URL be constructed using the following format:</t>

<sourcecode type="http"><![CDATA[https://<hostname>/<path>?approval-state=<base64-encoded-authorisation-data-object>
]]>
</sourcecode>

<section anchor="approval-redirect-url"><name>Approval Redirect URL</name>
<t>A registrar that supports multi-party authorization MUST provide a web-based consent screen to which the 3rd party can redirect the client's browser to obtain the registrant's approval for the requested operation. The registrar MUST provide a URL for this consent screen to the registry, which is used by the registry to inform the 3rd party where to redirect the client's browser.</t>
<t>Example registrar approval endpoint URL, with the <tt>approval-state</tt> query parameter containing the base64 encoded Authorisation Data Object:</t>

<sourcecode type="http"><![CDATA[HTTP/1.1 303 See Other
Location: https://registrar.example/rpp-auth/approve?approval-state=abc123
]]>
</sourcecode>
</section>

<section anchor="return-redirect-url"><name>Return Redirect URL</name>
<t>A 3rd party that supports multi-party authorization MUST provide a URL to which the client's browser is redirected by the registrar, after the registrar and registrant have completed the approval process. The 3rd party MUST provide this URL to the registry, which then appends it to the request, so that the registrar can redirect the client's browser back to the 3rd party after the approval process is completed.</t>
<t>Example 3rd party approval endpoint URL, with the <tt>approval-state</tt> query parameter containing the base64 encoded Authorisation Data Object:</t>

<sourcecode type="http"><![CDATA[HTTP/1.1 303 See Other
Location: https://thirdparty.example/rpp-auth/callback?approval-state=abc123
]]>
</sourcecode>
</section>
</section>
</section>

<section anchor="request-processing"><name>Request Processing</name>
<t>The request is initiated by the 3rd party, and validated and processed by both the registry and registrar. The request MUST be signed by both the registry and registrar.</t>

<section anchor="third-party"><name>Third Party</name>
<t>The 3rd party constructs the request, setting the <tt>transactionType</tt>, <tt>objectId</tt>, <tt>expiration</tt> and <tt>data</tt> properties, and sends the request to the registry.</t>
</section>

<section anchor="registry"><name>Registry</name>
<t>The registry, after validation of the request, adds the following properties to the request: <tt>id</tt>, <tt>requestorId</tt>, <tt>requestorName</tt>, <tt>approvalUrl</tt>, <tt>returnUrl</tt>, <tt>timestamp</tt>,<tt>signatures</tt>. The <tt>id</tt> is a unique identifier for the transaction. The <tt>requestorId</tt> is the identifier of the 3rd party that created the request, the <tt>requestorName</tt> is the public name of the requestor to be displayed in the consent screen. The <tt>approvalUrl</tt> is the URL where the request can be approved, the <tt>returnUrl</tt> is the URL to return to after approval, the <tt>timestamp</tt> is the time the request was created, the <tt>expiration</tt> is the time the request expires, the <tt>signatures</tt> contain the cryptographic signatures, the registry MUST add its signature of the request to the <tt>signatures</tt> array, it MUST be the first element in the array.</t>
</section>

<section anchor="registrar"><name>Registrar</name>
<t>The registrar adds the <tt>approval</tt> object to the request, to indicate whether the registrant and registrar have approved the transfer; at minimum, <tt>approval.approved</tt> and <tt>approval.approvedBy</tt> MUST be set. The registrar also adds its signature and public key identifier to the <tt>signatures</tt> array in the request, it MUST be the second element in the array. The registrar MUST verify the signature of the registry using the public key linked to the public key identifier in the request.</t>
</section>
</section>

<section anchor="approval-lifecycle"><name>Approval Lifecycle</name>
<t>The <tt>expiration</tt> property of the Authorisation Data Object indicates the time the approved request expires. The registry and registrar MUST reject any request that has expired, and return an appropriate error response to the 3rd party. The 3rd party MUST ensure that the request is submitted to the registry before it expires.</t>
<t>The registry MAY set a maximum expiration time for requests, and reject any request that exceeds this limit. The registrar MAY also set a maximum expiration time for requests, and reject any request that exceeds this limit.</t>
<t>The <tt>usage</tt> property of the Authorisation Data Object indicates whether the request is a single-use or multi-use request. The registry MUST keep track of the usage of the request, and reject any request that has already been used if it is a single-use request.</t>
<t>The 3rd party may rescind an approval it has previously granted, at any point while the authorisation remains valid, to immediately terminate its ongoing access to the object. The registry MUST check if the authorisation is linked to the 3rd party, and if so, it MUST invalidate the Authorisation Data Object and reject any subsequent RPP operation request that relies on it.</t>
<t>The registrar MAY rescind an approval it has previously granted, at any point while the authorisation remains valid, to immediately terminate the 3rd party's ongoing access to the object. This is particularly relevant for <tt>&quot;multi-use&quot;</tt> requests, which by design grant the 3rd party continuing access to perform the authorised operation on the object until the request expires or is rescinded.</t>
<t>To rescind an approval, the registrar sends a revocation request to the registry, identifying the Authorisation Data Object by its <tt>id</tt> and signed by the registrar using the same key used to approve the original request. Upon receiving a valid, signed revocation request, the registry MUST immediately invalidate the referenced Authorisation Data Object, regardless of its <tt>expiration</tt> or remaining uses, and MUST reject any subsequent RPP operation request that relies on it.</t>
<t>The registrar MAY rescind an approval, for any object under its management, for any reason, including but not limited to: the registrant revoking consent, a change in the registrant's circumstances, or a violation of the registrar's policies by the 3rd party. The registry MAY also rescind an approval if it determines that the 3rd party is no longer accredited or if it detects suspicious or malicious activity.</t>
<t>The registry MUST inform the 3rd party that the authorisation has been rescinded, so that the 3rd party can stop relying on it and, where applicable, notify its own client.</t>
</section>

<section anchor="request-signing-and-verification"><name>Request Signing and Verification</name>
<t>Both the registry and the registrar MUST cryptographically sign, and verify the authenticity and integrity of the requests. The specific algorithms and key management practices are outside the scope of this document, but it is RECOMMENDED that parties follow best practices for cryptographic security.</t>
<t>Each party MUST sign only the data that it is responsible for, and MUST include its public key identifier in the request, so that other parties can verify the signature using the corresponding public key.</t>

<section anchor="public-keys"><name>Public Keys</name>
<t>A registrar that wants to participate in multi-party authorization MUST provide at least one public key to the registry. Each key is identified by a unique key identifier, which is used to verify signatures applied by the corresponding private key.</t>
<t>The RPP server MUST provide the list of its own public keys and their identifiers to the 3rd party and the registrar, using the discovery document available at the well-known endpoint as described in <xref target="I-D.ietf-rpp-core"></xref>. The Discovery document MUST be extended to include the additional public keys and their identifiers, using the field name <tt>registryKeys</tt>, which is an array of JSON Web Key (JWK) objects <xref target="RFC7517"></xref>, the <tt>kid</tt> field of each JWK object MUST be used as the key identifier.</t>
<t>The <tt>use</tt> field of each JWK object MUST be set to <tt>sig</tt>, indicating that the key is used for digital signatures. The <tt>alg</tt> field of each JWK object MUST be set to the algorithm used to sign requests, such as <tt>RS256</tt> for RSA signatures using SHA-256.</t>
<t>The following example shows a discovery document with 2 registry public keys:</t>

<sourcecode type="json"><![CDATA[{
  "registryKeys": [
    {
      "kty": "RSA",
      "kid": "registry-key-1",
      "use": "sig",
      "alg": "RS256",
      "n": "0vx7agoebGcQSuuPiLJXZptN9nndrQmbXEps2aiAFbWhM78LhWx4cbb....",
      "e": "AQAB"
    },
    {
      "kty": "EC",
      "kid": "registry-key-2",
      "use": "sig",
      "alg": "ES256",
      "crv": "P-256",
      "x": "f83OJ3D2xF1Bg8vub9tLe1gHMzV76e8Tus9uPHvRVEU",
      "y": "x_FEzRu9m36HLN_tue659LNpXW6pCyStikYjKIWI5a0"
    }
  ]
}
]]>
</sourcecode>

<section anchor="json-schema-1"><name>JSON Schema</name>
<t><strong>TODO</strong></t>
</section>
</section>

<section anchor="canonicalization-and-signing-input"><name>Canonicalization and Signing Input</name>
<t>Signers MUST NOT construct the signature input by naively concatenating field values as strings. Ad hoc concatenation is ambiguous: for example, concatenating the values &quot;ab&quot; and &quot;c&quot; produces the same string as concatenating &quot;a&quot; and &quot;bc&quot;, and differences in whitespace, member ordering, or numeric formatting between a signer's and a verifier's JSON serializer can cause a correctly-signed request to fail verification.</t>
<t>Instead, the signature input MUST be derived by applying the JSON Canonicalization Scheme (JCS) <xref target="RFC8785"></xref> to a JSON object containing exactly the fields covered by that signature, encoded as UTF-8 octets. JCS produces a single, deterministic byte string for a given set of field values, independent of the original member order or formatting, making the signature input reproducible by any conforming verifier.</t>
<t>A signature MUST NOT cover the signature or key identifier fields of any other party. Chaining a later signature over earlier signatures would add no additional protection. Each party MUST include its own public key identifier alongside its signature, so that other parties can verify the signature using the corresponding public key.</t>
</section>

<section anchor="registry-1"><name>Registry</name>
<t>The registry MUST verify the transaction request, and copy the document to a response document while adding additional transaction related properties, and finally sign the response document using its private key. The signature MUST be included in the response object.</t>
<t>The registry signs the JCS-canonicalized form of:</t>

<ul spacing="compact">
<li>transactionType</li>
<li>id</li>
<li>timestamp</li>
<li>expiration</li>
<li>objectId</li>
<li>requestorId</li>
<li>approvalUrl</li>
<li>returnUrl</li>
<li>data</li>
</ul>
<t>The registry inserts the signature as an object in the <tt>signatures</tt> array.</t>
</section>

<section anchor="registrar-1"><name>Registrar</name>
<t>The registrar MUST verify the signature of the registry using the public key linked to the public key identifier found in the first element of the <tt>signatures</tt> array, and copy the request to a response document while adding additional transaction related properties, and finally sign the response using its private key. The signature MUST be included in the response object.</t>
<t>The registrar signs the JCS-canonicalized form of all properties also signed by the registry, but it MUST include the following additional properties in the signature input:</t>

<ul spacing="compact">
<li>approval</li>
<li>...</li>
</ul>
<t>The registrar inserts the signature as an object in the <tt>signatures</tt> array.</t>
</section>
</section>

<section anchor="transaction-types"><name>Transaction Types</name>
<t>A transaction type allows access to specific RPP operations. This specification defines three distinct transaction types. Future specifications may define additional transaction types, which MUST be registered with IANA as described in <xref target="iana-considerations"></xref>.</t>

<ul spacing="compact">
<li><tt>rpp:mpa-type:domain:ns-update</tt></li>
<li><tt>rpp:mpa-type:domain:dnssec-update</tt></li>
<li><tt>rpp:mpa-type:domain:transfer</tt></li>
</ul>
<t>This specification does not define a separate payload format for the RPP operation being authorized. Instead, the <tt>data</tt> property of a transaction request MUST contain the same JSON request message that would normally be sent directly to the RPP server for the corresponding operation, using the mapping rules and object representations defined in <xref target="I-D.ietf-rpp-json"></xref>.</t>

<section anchor="domain-name-server-update"><name>Domain Name Server Update</name>
<t>The <tt>rpp:mpa-type:domain:ns-update</tt> transaction type is used to update the delegation details for a domain name, by replacing the set of Host Data Objects referenced in the domain's <tt>nameservers</tt> property with a new set of Host Data Objects. The <tt>objectId</tt> property MUST contain the domain name of the target Domain Name Object.</t>
<t>The <tt>data</tt> property MUST contain a valid RPP JSON partial update request body (a JSON Patch document, as defined in the Partial Update rules of <xref target="I-D.ietf-rpp-json"></xref>), consisting of one <tt>replace</tt> operation for each existing Host Data Object being replaced. Each operation's <tt>path</tt> MUST be <tt>/nameservers</tt>, its <tt>match</tt> property MUST identify the existing Host Data Object being replaced by its <tt>hostName</tt>, and its <tt>value</tt> property MUST contain the new Host Data Object.</t>
<t><strong>TODO</strong></t>
</section>

<section anchor="domain-dnssec-update"><name>Domain DNSSEC Update</name>
<t>The <tt>rpp:mpa-type:domain:dnssec-update</tt> transaction type is used to update the DNSSEC related records of a domain name, by replacing the set of DS or DNSKEY records referenced in the domain's <tt>dns</tt> property with a new set of DS or DNSKEY records. The <tt>objectId</tt> property MUST contain the domain name of the target Domain Name Object.</t>
<t>The <tt>data</tt> property MUST contain a valid RPP JSON partial update request body (a JSON Patch document, as defined in the Partial Update rules of <xref target="I-D.ietf-rpp-json"></xref>).</t>
<t><strong>TODO</strong></t>
</section>

<section anchor="domain-transfer"><name>Domain Transfer</name>
<t>The <tt>rpp:mpa-type:domain:transfer</tt> transaction type is used to transfer the sponsorship of a domain to another registrar. The <tt>objectId</tt> property MUST contain the domain name of the target Domain Name Object, and the <tt>data</tt> property MUST contain a valid Transfer Process Object create request body, as defined in <xref target="I-D.ietf-rpp-json"></xref>, for the transfer operation described in <xref target="I-D.ietf-rpp-core"></xref>.</t>
<t><strong>TODO</strong></t>
</section>
</section>

<section anchor="http"><name>HTTP</name>

<section anchor="endpoints"><name>Endpoints</name>
<t>The following non normative table lists RPP endpoints related to Authorisation Objects, each derived by applying the rules defined in section &quot;HTTP Mapping Rules&quot; in <xref target="I-D.ietf-rpp-core"></xref>.</t>
<table><name>Authorisation Endpoints
{#tbl-authorisation-endpoints}
{#tbl-authorisation-process-endpoints}
</name>
<thead>
<tr>
<th>Operation</th>
<th>HTTP Method</th>
<th>URL path</th>
</tr>
</thead>

<tbody>
<tr>
<td>Authorisation: create</td>
<td><tt>&quot;POST&quot;</tt></td>
<td><tt>&quot;/{collection}/{id}/authorisations&quot;</tt></td>
</tr>

<tr>
<td>Authorisation: read</td>
<td><tt>&quot;GET&quot;</tt></td>
<td><tt>&quot;/{collection}/{id}/authorisations/latest&quot;</tt></td>
</tr>

<tr>
<td>Authorisation: read</td>
<td><tt>&quot;GET&quot;</tt></td>
<td><tt>&quot;/{collection}/{id}/authorisations/{id}&quot;</tt></td>
</tr>

<tr>
<td>Authorisation: delete</td>
<td><tt>&quot;DELETE&quot;</tt></td>
<td><tt>&quot;/{collection}/{id}/authorisations/{id}&quot;</tt></td>
</tr>

<tr>
<td>Authorisation: list</td>
<td><tt>&quot;GET&quot;</tt></td>
<td><tt>&quot;/{collection}/{id}/authorisations&quot;</tt></td>
</tr>
</tbody>
</table><t>The following non normative table lists RPP endpoints related to authorisation processes, each derived by applying the rules defined in section &quot;HTTP Mapping Rules&quot; in <xref target="I-D.ietf-rpp-core"></xref>.</t>
<table anchor="tbl-authorisation-process-endpoints"><name>Authorisation Process Endpoints</name>
<thead>
<tr>
<th>Operation</th>
<th>HTTP Method</th>
<th>URL path</th>
</tr>
</thead>

<tbody>
<tr>
<td>Authorisation Process: create</td>
<td><tt>&quot;POST&quot;</tt></td>
<td><tt>&quot;/{collection}/{id}/processes/authorisationProcesses&quot;</tt></td>
</tr>

<tr>
<td>Authorisation Process: read</td>
<td><tt>&quot;GET&quot;</tt></td>
<td><tt>&quot;/{collection}/{id}/processes/authorisationProcesses/latest&quot;</tt></td>
</tr>

<tr>
<td>Authorisation Process: read</td>
<td><tt>&quot;GET&quot;</tt></td>
<td><tt>&quot;/{collection}/{id}/processes/authorisationProcesses/{id}&quot;</tt></td>
</tr>

<tr>
<td>Authorisation Process: list</td>
<td><tt>&quot;GET&quot;</tt></td>
<td><tt>&quot;/{collection}/{id}/processes/authorisationProcesses&quot;</tt></td>
</tr>
</tbody>
</table><t><strong>TODO</strong></t>
</section>
</section>

<section anchor="examples"><name>Examples</name>

<section anchor="key-provisioning"><name>Key provisioning</name>
<t>A registrar supporting multi-party authorization MUST provide at least 1 public key at the registry. Public keys are provisioned by adding a Public Key Object to the <tt>publicKeys</tt> property of the Organisation Data Object <xref target="I-D.ietf-rpp-data-objects"></xref>, using an RPP JSON partial update (JSON Patch, as defined in the Partial Update rules of <xref target="I-D.ietf-rpp-json"></xref>). Since <tt>publicKeys</tt> is a <tt>DictionaryComposition[Public Key Object]</tt>, adding a new key MUST be done using an <tt>add</tt> operation whose <tt>path</tt> targets the new key identifier directly; the <tt>match</tt> property is not used, as it only applies to array-valued properties.</t>
<t>The following example shows a partial update request adding an RSA public key with identifier <tt>registrar-key-3</tt> to the <tt>publicKeys</tt> property of the Organisation Data Object with id <tt>ORG-12345</tt>:</t>

<sourcecode type="http"><![CDATA[PATCH /organisations/me HTTP/1.1
Authorization: Bearer <access-token>
Content-Type: application/json
[
  {
    "op": "add",
    "path": "/publicKeys",
    "value": {
      "registrar-key-3": {
          "@type": "publicKey",
          "type": "RSA",
          "alg": "RS256",
          "n": "0vx7agoebGcQSuuPiLJXZptN9nndrQmbXEps2aiAFbWhM78LhWx4cbb....",
          "e": "AQAB"
        }
    }
  }
]
]]>
</sourcecode>
<t><strong>TODO</strong> do we want to define &quot;me&quot; as a special identifier for the org of the current loggedin user, makes things easier for clients, will need to update core doc.</t>
</section>

<section anchor="redirection-url-management"><name>Redirection URL management</name>
<t>The registrar's <tt>approvalUrl</tt> and the 3rd party's <tt>returnUrl</tt> of their respective Organisation Data Object MUST be set, and MUST be valid URLs. Updating these URLs is done using an RPP JSON partial update of the Organisation Data Object, using a <tt>replace</tt> operation for each URL. The following example shows a partial update request to set the <tt>approvalUrl</tt> of the registrar's Organisation Data Object with id <tt>ORG-12345</tt>:</t>

<sourcecode type="http"><![CDATA[PATCH /organisations/ORG-12345 HTTP/1.1
Authorization: Bearer <access-token>
Content-Type: application/json
[
  {
    "op": "replace",
    "path": "/approvalUrl",   
    "value": "https://registrar.example/consent"
  }
]
]]>
</sourcecode>
<t><strong>TODO</strong></t>
</section>

<section anchor="domain-name-server-update-request"><name>Domain Name Server Update Request</name>
<t>Example 3rd party request to registry to replace the nameservers of a domain object with a new set of Host Data Objects. The <tt>data</tt> property contains an RPP JSON partial update (JSON Patch) request body as defined in <xref target="I-D.ietf-rpp-json"></xref>, with one <tt>replace</tt> operation for the existing nameservers. The example below shows a request to update the domain <tt>test1.example</tt>, replacing the existing nameservers with <tt>ns1.dns.example</tt> and <tt>ns2.dns.example</tt>:</t>

<sourcecode type="json"><![CDATA[{
  "@type": "authorisation",
  "transactionType": "rpp:mpa-type:domain:ns-update",
  "timestamp": "2027-06-01T12:00:00Z",
  "expiration": "2027-06-01T12:10:00Z",
  "objectId": "test1.example",
  "data": [
    {
      "op": "replace",
      "path": "/nameservers",
      "value": [
        { "@type": "host", "hostName": "ns1.dns.example" },
        { "@type": "host", "hostName": "ns2.dns.example" }
      ] 
    }
  ]
}
]]>
</sourcecode>
<t>Example Registry Response: <strong>TODO</strong></t>
<t>Example Registrar Response: <strong>TODO</strong></t>
</section>

<section anchor="domain-dnssec-update-request"><name>Domain DNSSEC Update Request</name>
<t>Example 3rd party request to registry to replace the DNSSEC DS records of a domain object with a new set of DS records. The <tt>data</tt> property contains an RPP JSON partial update (JSON Patch) request body as defined in <xref target="I-D.ietf-rpp-json"></xref>, with one <tt>replace</tt> operation for the existing DS records. The example below shows a request to update the domain <tt>test1.example</tt>, replacing the existing DS records with a new DS record with key tag <tt>12345</tt>:</t>

<sourcecode type="json"><![CDATA[{
  "@type": "authorisation",
  "transactionType": "rpp:mpa-type:domain:dnssec-update",
  "timestamp": "2027-06-01T12:00:00Z",
  "expiration": "2027-06-01T12:10:00Z",
  "objectId": "test1.example",
  "data": [
    {
      "op": "replace",
      "path": "/dns",
      "value": {
        "@type": "dnsData",
        "records": [
            { "@type": "dnsRecord", 
              "name": "@",
              "type": "ds",
              "rdata": { "keyTag": 12345,
                         "algorithm": 8,
                         "digestType": 2,
                         "digest": "ABCDEF1234567890"
                       }
            }
        ]
      }
    }
  ]
}
]]>
</sourcecode>
<t>Example Registry Response: <strong>TODO</strong></t>
<t>Example Registrar Response: <strong>TODO</strong></t>
</section>

<section anchor="domain-transfer-request"><name>Domain Transfer Request</name>
<t>Example 3rd party request to registry to transfer the management of a domain object. The <tt>data</tt> property contains a Transfer Process Object as defined in <xref target="I-D.ietf-rpp-json"></xref>:</t>

<sourcecode type="json"><![CDATA[{
  "@type": "authorisation",
  "transactionType": "rpp:mpa-type:domain:transfer",
  "id": "TR-12345",
  "timestamp": "2027-06-01T12:00:00Z",
  "expiration": "2027-06-01T12:10:00Z",
  "objectId": "test1.example",
  "data": {
    "@type": "transferProcess",
    "transferDir": "pull"
  }
}
]]>
</sourcecode>
<t>Example registry response to the 3rd party to transfer the management of a domain object, signed by the registry, this response is then sent to the losing registrar, by the 3rd party, to request the registrar and registrant's approval for the transfer.</t>
<t>The registry adds the following properties to the request: <tt>id</tt>, <tt>requestorId</tt>, <tt>requestorName</tt>, and <tt>signatures</tt>. The <tt>id</tt> is a unique identifier for the transaction, the <tt>requestorId</tt> is the identifier of the 3rd party that created the request, the <tt>requestorName</tt> is the public name of the requestor to be displayed in the consent screen, and the <tt>signatures</tt> array contains the registry's signature and public key identifier, as the first element in the array.</t>

<sourcecode type="json"><![CDATA[{
  "@type": "authorisation",
  "transactionType": "rpp:mpa-type:domain:transfer",
  "id": "TR-12345",
  "timestamp": "2027-06-01T12:00:00Z",
  "expiration": "2027-06-01T12:10:00Z",
  "objectId": "test1.example",
  "requestorId": "ORG-3RDPARTY-1",
  "requestorName": "Example Registrar",
  "data": {
    "@type": "transferProcess",
    "transferDir": "pull"
  },
  "signatures": [
    {
      "keyId": "my-registry-key-1",
      "signedAt": "2027-06-01T12:00:00Z",
      "signature": "yyyyy"
    }
  ]
}
]]>
</sourcecode>
<t>Example response from losing registrar, signed by the registrar and the <tt>approval</tt> object contains the result of the approval process, to indicate whether the registrant and registrar have approved the transfer. The registrar also adds its signature and public key identifier to the <tt>signatures</tt> array in the response.</t>
<t>This response is sent to the registry to request execution of the transfer:</t>

<sourcecode type="json"><![CDATA[{
  "@type": "authorisation",
  "transactionType": "rpp:mpa-type:domain:transfer",
  "id": "TR-12345",
  "timestamp": "2027-06-01T12:00:00Z",
  "expiration": "2027-06-01T12:10:00Z",
  "objectId": "test1.example",
  "requestorId": "ORG-3RDPARTY-1",
  "requestorName": "Example Registrar",
  "approval": {
    "approved": true,
    "approvedBy": "registrar-admin@example-registrar.example",
    "timestamp": "2027-06-01T12:05:00Z"
  },
  "data": {
    "@type": "transferProcess",
    "transferDir": "pull"
  },
  "signatures": [
    {
      "keyId": "my-registry-key-1",
      "signedAt": "2027-06-01T12:00:00Z",
      "signature": "yyyyy"
    },
    {
      "keyId": "my-registrar-key-1",
      "signedAt": "2027-06-01T12:05:00Z",
      "signature": "zzzzz"
    }
  ]
}
]]>
</sourcecode>
<t>This response is used by the 3rd party to submit the transfer request to the registry, The registry verifies the signatures of the registry and registrar, and confirms that <tt>approval.approved</tt> is set to <tt>true</tt>. If both signatures are valid and the approval is granted, the registry MUST create an authorization object, allowing the transfer operation to be executed, and return a success response to the 3rd party. The 3rd party can then submit the transfer request to the registry, which will execute the transfer operation.</t>
</section>
</section>

<section anchor="security-considerations"><name>Security Considerations</name>
<t>The registrant's explicit consent is required for any RPP operation to be executed. The registrant's consent is obtained through the registrar's web-based consent screen, which is presented to the registrant by the registrar. The registrar MUST ensure that the registrant's consent is obtained before any RPP operation is executed.</t>
<t>The registry MUST ensure that the registrant is the owner of the domain name for which the RPP operation is requested, and that the registrant's consent is obtained before any RPP operation is executed.</t>
<t>The registry MUST ensure that the registrar is the current sponsoring registrar of record for the domain name for which the RPP operation is requested, the sponsoring registrar may have changed between the time the request is created and the time the RPP operation, using the authorization request, is executed.</t>
<t>The registry MUST check if the authorization request used to request an RPP operation is correctly signed by the registry itself and the registrar, and that the request is valid and not expired. The registry MUST reject any request with a missing or invalid signature, or that is invalid or expired. The Registry MUST check that the authorization request is used only once, when the <tt>use</tt> property is set to <tt>single-use</tt>. The registry MUST ensure that the authorization request is used only for the specific RPP operation for which it was created, and that the request is not used to authorize any other operation.</t>
<t>Signature verification MUST be performed locally by each verifying party using the public key of the signer, rather than by querying the signer's system at verification time. In particular, the registrar MUST verify the registry's signature (and the registry's verification of the 3rd party's signature) using the registry's public key, and MUST NOT rely on a live call back to the registry to confirm that a signature is valid. The registry already re-verifies all signatures, when the fully-signed request is submitted for execution (see <xref target="architectural-overview"></xref>).</t>
<t>This document does not specify any authorization and authentication mechanisms for the 3rd party, registry, and registrar to secure the HTTP endpoints used to exchange requests and responses. It is RECOMMENDED to use OAuth 2.0 for RPP, as described in <xref target="I-D.wullink-rpp-oauth2"></xref>.</t>
<t>The cryptographic algorithms and key management practices used to sign and verify requests are outside the scope of this document, but it is RECOMMENDED that parties follow best practices for cryptographic security.</t>
<t><strong>TODO</strong></t>
</section>

<section anchor="result-codes"><name>Result Codes</name>
<t>This specification defines new RPP result codes for multi-party authorization, the new result codes follow the result code classes defined in <xref target="I-D.ietf-rpp-core"></xref>: <tt>14xxx</tt> for client errors and <tt>15xxx</tt> for server errors. As described in <xref target="I-D.ietf-rpp-core"></xref>.</t>

<section anchor="client-errors"><name>Client Errors</name>
<t>The following client error result codes (class <tt>14xxx</tt>) are defined in this document:</t>
<table anchor="tbl-rpp-client-errors"><name>RPP multi-party authorization client error result codes</name>
<thead>
<tr>
<th>RPP Result Code</th>
<th>HTTP Status Code</th>
<th>Description</th>
</tr>
</thead>

<tbody>
<tr>
<td>14001</td>
<td>400 Bad Request</td>
<td>The <tt>transactionType</tt> is missing, unknown, or not registered in the &quot;RPP Multi-Party Transaction Types&quot; registry <xref target="tbl-rpp-transaction-types"></xref>.</td>
</tr>

<tr>
<td>14002</td>
<td>400 Bad Request</td>
<td>The <tt>data</tt> property does not conform to the RPP JSON request body expected for the given <tt>transactionType</tt>.</td>
</tr>

<tr>
<td>14003</td>
<td>404 Not Found</td>
<td>The <tt>objectId</tt> does not identify an existing object.</td>
</tr>

<tr>
<td>14004</td>
<td>403 Forbidden</td>
<td>One or more required signatures are missing or fail cryptographic verification.</td>
</tr>

<tr>
<td>14005</td>
<td>400 Bad Request</td>
<td>A <tt>keyId</tt> referenced in the request does not match any public key currently provisioned by the identified party.</td>
</tr>

<tr>
<td>14006</td>
<td>403 Forbidden</td>
<td>The 3rd party is not accredited by the registry to perform the requested operation on the referenced object.</td>
</tr>

<tr>
<td>14007</td>
<td>403 Forbidden</td>
<td>The identified registrar is not the current sponsoring registrar of record for the referenced object.</td>
</tr>

<tr>
<td>14008</td>
<td>400 Bad Request</td>
<td>The <tt>expiration</tt> timestamp of the request has already passed.</td>
</tr>

<tr>
<td>14009</td>
<td>409 Conflict</td>
<td>The <tt>id</tt> of the request has already been used by a previously completed or in-progress transaction.</td>
</tr>

<tr>
<td>14010</td>
<td>403 Forbidden</td>
<td>The registrant did not approve the requested operation at the registrar's consent screen.</td>
</tr>
</tbody>
</table><t><strong>TODO</strong> complete the table with additional result codes</t>
</section>

<section anchor="server-errors"><name>Server Errors</name>
<t>The following server error result codes (class <tt>15xxx</tt>) are defined in this document:</t>
<table anchor="tbl-rpp-server-errors"><name>RPP multi-party authorization server error result codes</name>
<thead>
<tr>
<th>RPP Result Code</th>
<th>HTTP Status Code</th>
<th>Description</th>
</tr>
</thead>

<tbody>
<tr>
<td>15001</td>
<td>500 Internal Server Error</td>
<td>The registry was unable to sign the authorization request.</td>
</tr>

<tr>
<td>15002</td>
<td>500 Internal Server Error</td>
<td>The registry was unable to retrieve or validate a registrar's public key.</td>
</tr>
</tbody>
</table><t><strong>TODO</strong> complete the table with additional result codes</t>
</section>
</section>

<section anchor="iana-considerations"><name>IANA Considerations</name>
<t>IANA is requested to register the result codes defined in <xref target="tbl-rpp-client-errors"></xref> and <xref target="tbl-rpp-server-errors"></xref> in the &quot;RPP Result codes&quot; registry defined in <xref target="I-D.ietf-rpp-core"></xref>.</t>
<t>IANA is requested to create a new registry for RPP multi-party transaction type identifiers, these identifiers are used to identify the type of RPP transaction used by 3rd parties to request authorization from the registry and registrar. The registry is to be called &quot;RPP Multi-Party Transaction Types&quot; and is to be maintained by the IETF RPP working group.</t>

<sourcecode type="text"><![CDATA[Name of the registry: RPP Multi-Party Transaction Types
Registry group: RESTful Provisioning Protocol (RPP)
Registration procedure: Expert Review
]]>
</sourcecode>
<t>The following transaction types are defined in this document:</t>
<table anchor="tbl-rpp-transaction-types"><name>RPP multi-party authorization transaction types</name>
<thead>
<tr>
<th>Identifier</th>
<th>Description</th>
</tr>
</thead>

<tbody>
<tr>
<td>rpp:mpa-type:domain:ns-update</td>
<td>Update DNS hosting (nameservers) for a domain</td>
</tr>

<tr>
<td>rpp:mpa-type:domain:dnssec-update</td>
<td>Update DNSSEC DS records for a domain</td>
</tr>

<tr>
<td>rpp:mpa-type:domain:transfer</td>
<td>Transfer a domain to another registrar</td>
</tr>
</tbody>
</table><t><strong>TODO</strong></t>
</section>

<section anchor="internationalization-considerations"><name>Internationalization Considerations</name>
<t><strong>TODO</strong></t>
</section>

<section anchor="privacy-considerations"><name>Privacy Considerations</name>
<t>The registrant personally identifiable information (PII) is not included in the multi-party authorization flow, except for the registrant's explicit consent obtained through the registrar's web-based consent screen.</t>
<t><strong>TODO</strong></t>
</section>

<section anchor="change-history"><name>Change History</name>
<t><strong>TODO</strong></t>
</section>

</middle>

<back>
<references><name>References</name>
<references><name>Normative References</name>
<xi:include href="https://bib.ietf.org/public/rfc/bibxml3/reference.I-D.ietf-rpp-core.xml"/>
<xi:include href="https://bib.ietf.org/public/rfc/bibxml3/reference.I-D.ietf-rpp-data-objects.xml"/>
<xi:include href="https://bib.ietf.org/public/rfc/bibxml3/reference.I-D.ietf-rpp-json.xml"/>
<xi:include href="https://bib.ietf.org/public/rfc/bibxml3/reference.I-D.wullink-rpp-oauth2.xml"/>
<xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.2119.xml"/>
<xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.3986.xml"/>
<xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.7517.xml"/>
<xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.8785.xml"/>
</references>
<references><name>Informative References</name>
<reference anchor="JSON-SCHEMA" target="https://json-schema.org/draft/2020-12/json-schema-core">
  <front>
    <title>JSON Schema: A vocabulary for describing JSON Data</title>
    <author>
      <organization>JSON Schema</organization>
    </author>
    <date year="2020"></date>
  </front>
</reference>
</references>
</references>

<section anchor="acknowledgements" numbered="false"><name>Acknowledgements</name>
<t><strong>TODO</strong></t>
</section>

</back>

</rfc>
